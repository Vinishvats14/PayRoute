PayRoute Frontend

Quick start:
1. cd frontend
2. npm install
3. npm run dev

Configure API base URL with environment variable VITE_API_URL (defaults to http://localhost:8080).

This is a minimal scaffold (Vite + React Router + Axios). Extend components and styling as needed.
